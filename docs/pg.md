# PostgreSQL 本地数据库参考

## 连接信息

| 参数 | 值 |
|------|-----|
| 主机 | localhost |
| 端口 | 5432 |
| 用户名 | postgres |
| 密码 | postgres |
| 数据库 | postgres |

## 常用命令

### 登录数据库

```bash
# 方式一：使用 psql 命令行
PGPASSWORD=postgres psql -h localhost -U postgres -d postgres

# 方式二：使用连接字符串
psql postgres://postgres:postgres@localhost:5432/postgres
```

### 常用查询命令

```sql
-- 列出所有表
\dt

-- 查看表结构
\d <表名>
\d teams

-- 查看表数据
SELECT * FROM <表名> LIMIT 10;

-- 查看表行数
SELECT relname AS table_name, n_live_tup AS row_count
FROM pg_stat_user_tables ORDER BY relname;

-- 查看所有索引
\di

-- 查看表的外键关系
SELECT
  tc.table_name, kcu.column_name,
  ccu.table_name AS foreign_table_name,
  ccu.column_name AS foreign_column_name
FROM information_schema.table_constraints AS tc
JOIN information_schema.key_column_usage AS kcu ON tc.constraint_name = kcu.constraint_name
JOIN information_schema.constraint_column_usage AS ccu ON ccu.constraint_name = tc.constraint_name
WHERE tc.constraint_type = 'FOREIGN KEY';

-- 查看迁移历史
SELECT * FROM _migrations ORDER BY version_id;
```

---

## 表结构总览

共 17 张表，核心关系如下：

```
users ──┬──< access_tokens
        └──< users_teams >──── teams ──┬──< team_api_keys
                                       ├──< envs ──┬──< env_aliases
                                       │           ├──< env_build_assignments >── env_builds
                                       │           ├──< snapshot_templates
                                       │           └──< snapshots
                                       ├──< addons
                                       ├──< volumes
                                       └──── clusters
tiers ──< teams
_migrations (独立表)
active_template_builds (独立表)
```

---

## 各表详细结构与示例数据

### 1. `_migrations` — 数据库迁移记录

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| id | integer | NOT NULL | generated by default as identity |
| version_id | bigint | NOT NULL | |
| is_applied | boolean | NOT NULL | |
| tstamp | timestamp | NOT NULL | now() |

**主键**: `id`

**示例数据**:

| id | version_id | is_applied | tstamp |
|----|------------|------------|--------|
| 1 | 0 | t | 2026-03-31 02:05:08 |
| 2 | 20000101000000 | t | 2026-03-31 02:05:08 |
| 3 | 20000101000001 | t | 2026-03-31 02:05:08 |
| 4 | 20231124185944 | t | 2026-03-31 02:05:08 |
| 5 | 20231220094836 | t | 2026-03-31 02:05:08 |

---

### 2. `users` — 用户表

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| id | uuid | NOT NULL | |
| email | text | NOT NULL | |
| created_at | timestamptz | NOT NULL | now() |
| updated_at | timestamptz | NOT NULL | now() |

**主键**: `id`
**唯一索引**: `email`
**触发器**: `post_user_signup` — 用户注册后自动处理

**示例数据**:

| id | email | created_at |
|----|-------|------------|
| 00000000-0000-0000-0000-000000000000 | system@e2b.dev | 2026-03-31 02:05:09 |
| fb69f46f-eb51-4a87-a14e-306f7a3fd89c | user@e2b-dev.local | 2026-03-31 02:49:29 |

---

### 3. `teams` — 团队表

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| id | uuid | NOT NULL | gen_random_uuid() |
| name | text | NOT NULL | |
| email | varchar(255) | NOT NULL | |
| tier | text | NOT NULL | |
| slug | text | NOT NULL | |
| is_blocked | boolean | NOT NULL | false |
| is_banned | boolean | NOT NULL | false |
| blocked_reason | text | | |
| cluster_id | uuid | | |
| sandbox_scheduling_labels | text[] | NOT NULL | '{}' |
| profile_picture_url | text | | |
| created_at | timestamptz | NOT NULL | CURRENT_TIMESTAMP |

**主键**: `id`
**唯一索引**: `slug`
**外键**: `tier` → `tiers(id)`, `cluster_id` → `clusters(id)`
**触发器**: `team_slug_trigger` — 插入时自动生成 slug

**示例数据**:

| id | name | tier | email | slug |
|----|------|------|-------|------|
| 6ceff94b-... | system@e2b.dev | base_v1 | system@e2b.dev | system |
| 530a7533-... | user@e2b-dev.local | base_v1 | user@e2b-dev.local | user |
| 0b8a3ded-... | local-dev team | base_v1 | team@e2b-dev.local | local-dev-team |

---

### 4. `users_teams` — 用户-团队关联表 (多对多)

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| uuid_id | uuid | NOT NULL | gen_random_uuid() |
| id | bigint | NOT NULL | generated by default as identity |
| user_id | uuid | NOT NULL | |
| team_id | uuid | NOT NULL | |
| is_default | boolean | NOT NULL | false |
| added_by | uuid | | |
| created_at | timestamp | | CURRENT_TIMESTAMP |

**主键**: `uuid_id`
**唯一索引**: `(team_id, user_id)`
**外键**: `user_id` → `users(id)`, `team_id` → `teams(id)`, `added_by` → `users(id)`

**示例数据**:

| user_id | team_id | is_default |
|---------|---------|------------|
| 00000000-... (system) | 6ceff94b-... (system) | t |
| fb69f46f-... (user) | 530a7533-... (user team) | f |
| fb69f46f-... (user) | 0b8a3ded-... (local-dev) | t |

---

### 5. `tiers` — 套餐/层级表

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| id | text | NOT NULL | |
| name | text | NOT NULL | |
| disk_mb | bigint | NOT NULL | 512 |
| concurrent_instances | bigint | NOT NULL | |
| max_length_hours | bigint | NOT NULL | |
| max_vcpu | bigint | NOT NULL | 8 |
| max_ram_mb | bigint | NOT NULL | 8192 |
| concurrent_template_builds | bigint | NOT NULL | 20 |

**主键**: `id`
**约束**: `concurrent_instances > 0`, `concurrent_template_builds > 0`, `disk_mb > 0`

**示例数据**:

| id | name | disk_mb | concurrent_instances | max_length_hours | max_vcpu | max_ram_mb |
|----|------|---------|---------------------|------------------|----------|------------|
| base_v1 | Base tier | 512 | 20 | 1 | 8 | 8192 |

---

### 6. `access_tokens` — 用户访问令牌

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| id | uuid | NOT NULL | gen_random_uuid() |
| user_id | uuid | NOT NULL | |
| access_token_hash | text | NOT NULL | |
| name | text | NOT NULL | 'Unnamed Access Token' |
| access_token_prefix | varchar(10) | NOT NULL | |
| access_token_length | integer | NOT NULL | |
| access_token_mask_prefix | varchar(5) | NOT NULL | |
| access_token_mask_suffix | varchar(5) | NOT NULL | |
| created_at | timestamptz | NOT NULL | CURRENT_TIMESTAMP |

**主键**: `id`
**唯一索引**: `access_token_hash`
**外键**: `user_id` → `users(id)` ON DELETE CASCADE

**示例数据**:

| id | name | access_token_prefix | user_id |
|----|------|---------------------|---------|
| 3d98c426-... | local dev seed token | sk_e2b_ | fb69f46f-... |

---

### 7. `team_api_keys` — 团队 API 密钥

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| id | uuid | NOT NULL | gen_random_uuid() |
| team_id | uuid | NOT NULL | |
| api_key_hash | text | NOT NULL | |
| name | text | NOT NULL | 'Unnamed API Key' |
| api_key_prefix | varchar(10) | NOT NULL | |
| api_key_length | integer | NOT NULL | |
| api_key_mask_prefix | varchar(5) | NOT NULL | |
| api_key_mask_suffix | varchar(5) | NOT NULL | |
| created_by | uuid | | |
| last_used | timestamptz | | |
| created_at | timestamptz | NOT NULL | CURRENT_TIMESTAMP |
| updated_at | timestamptz | | |

**主键**: `id`
**唯一索引**: `api_key_hash`
**外键**: `team_id` → `teams(id)` ON DELETE CASCADE, `created_by` → `users(id)`

**示例数据**:

| id | name | api_key_prefix | team_id | last_used |
|----|------|----------------|---------|-----------|
| cba262da-... | local dev seed token | e2b_ | 0b8a3ded-... | 2026-04-09 07:04:04 |

---

### 8. `envs` — 环境/模板表

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| id | text | NOT NULL | |
| team_id | uuid | NOT NULL | |
| public | boolean | NOT NULL | false |
| build_count | integer | NOT NULL | 1 |
| spawn_count | bigint | NOT NULL | 0 |
| last_spawned_at | timestamptz | | |
| created_by | uuid | | |
| cluster_id | uuid | | |
| source | text | NOT NULL | 'template' |
| created_at | timestamptz | NOT NULL | CURRENT_TIMESTAMP |
| updated_at | timestamptz | NOT NULL | |

**主键**: `id`
**外键**: `team_id` → `teams(id)`, `created_by` → `users(id)`, `cluster_id` → `clusters(id)`

**示例数据**:

| id | team_id | build_count | spawn_count | source |
|----|---------|-------------|-------------|--------|
| 4u5f5e393rle2b3x70m1 | 0b8a3ded-... | 46 | 32 | template |

---

### 9. `env_builds` — 环境构建记录

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| id | uuid | NOT NULL | gen_random_uuid() |
| status | text | NOT NULL | 'waiting' |
| status_group | text | NOT NULL | (触发器计算) |
| dockerfile | text | | |
| start_cmd | text | | |
| ready_cmd | text | | |
| vcpu | bigint | NOT NULL | |
| ram_mb | bigint | NOT NULL | |
| free_disk_size_mb | bigint | NOT NULL | |
| total_disk_size_mb | bigint | | |
| kernel_version | text | NOT NULL | 'vmlinux-5.10.186' |
| firecracker_version | text | NOT NULL | |
| env_id | text | | |
| envd_version | text | | |
| cluster_node_id | text | | |
| reason | jsonb | NOT NULL | '{}' |
| version | text | | |
| cpu_architecture | text | | |
| cpu_family | text | | |
| cpu_model | text | | |
| cpu_model_name | text | | |
| cpu_flags | text[] | | |
| team_id | uuid | | |
| created_at | timestamptz | NOT NULL | CURRENT_TIMESTAMP |
| updated_at | timestamptz | NOT NULL | |
| finished_at | timestamptz | | |

**主键**: `id`
**触发器**: `trg_compute_status_group` — 根据 status 自动计算 status_group

**示例数据** (部分列):

| id | status | env_id | vcpu | ram_mb | reason |
|----|--------|--------|------|--------|--------|
| 25992c8d-... | failed | 4u5f5e393rle2b3x70m1 | 2 | 512 | {"message": ""} |
| de2aea62-... | failed | 4u5f5e393rle2b3x70m1 | 2 | 512 | {"message": "build was cancelled"} |
| d4e88d38-... | failed | 4u5f5e393rle2b3x70m1 | 2 | 512 | {"message": "An internal error occurred..."} |

---

### 10. `env_build_assignments` — 构建分配记录

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| id | uuid | NOT NULL | gen_random_uuid() |
| env_id | text | NOT NULL | |
| build_id | uuid | NOT NULL | |
| tag | text | NOT NULL | |
| source | text | NOT NULL | 'app' |
| created_at | timestamptz | | CURRENT_TIMESTAMP |

**主键**: `id`
**外键**: `build_id` → `env_builds(id)`, `env_id` → `envs(id)`
**触发器**: `trigger_backfill_env_id`, `trigger_backfill_team_id`

**示例数据**:

| id | env_id | build_id | tag | source |
|----|--------|----------|-----|--------|
| cfafb657-... | 4u5f5e393rle2b3x70m1 | 25992c8d-... | default | app |
| efe4b1e2-... | 4u5f5e393rle2b3x70m1 | d4e88d38-... | default | app |

---

### 11. `env_aliases` — 环境别名

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| id | uuid | NOT NULL | gen_random_uuid() |
| alias | text | NOT NULL | |
| is_renamable | boolean | NOT NULL | true |
| env_id | text | NOT NULL | |
| namespace | text | | |

**主键**: `id`
**唯一索引**: `(alias, namespace)` NULLS NOT DISTINCT
**外键**: `env_id` → `envs(id)` ON DELETE CASCADE

**示例数据**:

| alias | env_id | namespace |
|-------|--------|-----------|
| base | 4u5f5e393rle2b3x70m1 | local-dev-team |

---

### 12. `active_template_builds` — 活跃模板构建

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| build_id | uuid | NOT NULL | |
| team_id | uuid | NOT NULL | |
| template_id | text | NOT NULL | |
| tags | text[] | NOT NULL | |
| created_at | timestamptz | NOT NULL | now() |

**主键**: `build_id`

**示例数据**:

| build_id | team_id | template_id | tags |
|----------|---------|-------------|------|
| 25992c8d-... | 0b8a3ded-... | 4u5f5e393rle2b3x70m1 | {default} |
| 7cdb1422-... | 0b8a3ded-... | 4u5f5e393rle2b3x70m1 | {default} |

---

### 13. `clusters` — 集群配置

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| id | uuid | NOT NULL | gen_random_uuid() |
| endpoint | text | NOT NULL | |
| endpoint_tls | boolean | NOT NULL | true |
| token | text | NOT NULL | |
| sandbox_proxy_domain | text | | |

**主键**: `id`

**示例数据**: 无数据

---

### 14. `snapshots` — 快照表

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| id | uuid | NOT NULL | gen_random_uuid() |
| env_id | text | NOT NULL | |
| sandbox_id | text | NOT NULL | |
| base_env_id | text | NOT NULL | |
| metadata | jsonb | NOT NULL | '{}' |
| env_secure | boolean | NOT NULL | false |
| origin_node_id | text | NOT NULL | |
| allow_internet_access | boolean | | |
| auto_pause | boolean | NOT NULL | false |
| team_id | uuid | NOT NULL | |
| config | jsonb | | |
| sandbox_started_at | timestamptz | NOT NULL | |
| created_at | timestamptz | | now() |

**主键**: `id`
**唯一索引**: `sandbox_id`
**外键**: `env_id` → `envs(id)`, `base_env_id` → `envs(id)`, `team_id` → `teams(id)`
**触发器**: `trg_snapshots_fix_json_null_metadata`, `trg_sync_env_source_on_snapshot`

**示例数据**: 无数据

---

### 15. `snapshot_templates` — 快照模板

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| env_id | text | NOT NULL | |
| sandbox_id | text | NOT NULL | |
| origin_node_id | text | | |
| build_id | uuid | | |
| created_at | timestamptz | | now() |

**主键**: `env_id`
**外键**: `env_id` → `envs(id)` ON DELETE CASCADE

**示例数据**: 无数据

---

### 16. `addons` — 附加组件/配额扩展

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| id | uuid | NOT NULL | gen_random_uuid() |
| team_id | uuid | NOT NULL | |
| name | text | NOT NULL | |
| description | text | | |
| extra_concurrent_sandboxes | bigint | NOT NULL | 0 |
| extra_concurrent_template_builds | bigint | NOT NULL | 0 |
| extra_max_vcpu | bigint | NOT NULL | 0 |
| extra_max_ram_mb | bigint | NOT NULL | 0 |
| extra_disk_mb | bigint | NOT NULL | 0 |
| valid_from | timestamptz | NOT NULL | CURRENT_TIMESTAMP |
| valid_to | timestamptz | | |
| added_by | uuid | NOT NULL | |
| idempotency_key | text | | |

**主键**: `id`
**外键**: `team_id` → `teams(id)`, `added_by` → `users(id)`
**约束**: `valid_to IS NULL OR valid_to > valid_from`

**示例数据**: 无数据

---

### 17. `volumes` — 存储卷

| 列名 | 类型 | 可空 | 默认值 |
|------|------|------|--------|
| id | uuid | NOT NULL | gen_random_uuid() |
| team_id | uuid | NOT NULL | |
| name | varchar(250) | NOT NULL | |
| volume_type | varchar(250) | NOT NULL | |
| created_at | timestamptz | NOT NULL | CURRENT_TIMESTAMP |

**主键**: `id`
**唯一索引**: `(team_id, name)`
**外键**: `team_id` → `teams(id)`

**示例数据**: 无数据

---

## 本地开发种子数据

本地开发环境预置了以下种子数据：

| 实体 | 说明 |
|------|------|
| 系统用户 | `system@e2b.dev` (id: 00000000-...) |
| 本地用户 | `user@e2b-dev.local` (id: fb69f46f-...) |
| 本地团队 | `local-dev team` (slug: local-dev-team, id: 0b8a3ded-...) |
| API Key | 前缀 `e2b_`，关联到 local-dev team |
| Access Token | 前缀 `sk_e2b_`，关联到本地用户 |
| 基础套餐 | `base_v1`: 20 并发实例, 8 vCPU, 8GB RAM, 512MB 磁盘 |
| 基础环境 | `4u5f5e393rle2b3x70m1` (alias: base) |
